# Changelog

## 0.1.1

- Fixed & Refined: Custom exception exports (`InputError`, `ConfigurationError`, `DataError`, `AnalysisError`, `ReportError`) in root namespace.
- Added: Comprehensive pipeline input validation tests (`tests/test_validation_and_errors.py`).
- Added: Full documentation suite under `docs/` and runnable examples (`examples/`).
- Improved: Engine analysis pipeline validation and error handling diagnostics.

## 0.1.0

- Initial MVP: explore/analyze pipeline, ten builtin analyses, HTML reports, CLI, tests, benchmark harness without fabricated scores.
